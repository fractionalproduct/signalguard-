# Use a flat (hoisted) node_modules layout. Next.js's production build resolves a
# single copy of React/react-dom this way; pnpm's default isolated layout causes
# the "<Html> should not be imported" / null-useContext errors when Next prerenders
# its built-in 404/500 pages in a monorepo.
node-linker=hoisted
